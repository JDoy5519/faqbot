# Duplicate to .env.local and fill with real keys (never commit .env.local)

OPENAI_API_KEY=sk-REPLACE

NEXT_PUBLIC_SUPABASE_URL=https://REPLACE.supabase.co
SUPABASE_SERVICE_ROLE_KEY=REPLACE

STRIPE_SECRET_KEY=sk_test_REPLACE
STRIPE_WEBHOOK_SECRET=whsec_REPLACE
NEXT_PUBLIC_STRIPE_PRICE_STARTER=price_REPLACE
NEXT_PUBLIC_STRIPE_PRICE_PRO=price_REPLACE

JWT_SECRET=replace-with-long-random-string

RESEND_API_KEY=re_REPLACE

REDIS_URL=redis://default:REPLACE@redis-host:port

